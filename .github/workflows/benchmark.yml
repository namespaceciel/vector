name: benchmark

on:
  push:
    branches:
      - main

jobs:
  job:
    name: benchmark against ${{ matrix.compiler.name }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compiler:
          - { name: "gcc", path: "g++", flags: "" }
          - { name: "clang", path: "clang++", flags: "-stdlib=libc++" }

    steps:
      - uses: actions/checkout@v4

      - name: benchmark against ${{ matrix.compiler.name }}
        run: |
          make benchmark COMPILER_PATH="${{ matrix.compiler.path }}" COMPILER_FLAGS="${{ matrix.compiler.flags }}"
